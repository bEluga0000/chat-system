import { Typography } from "@mui/material";

export default function ShowRoom()
{
    return(
        <div style={{padding:'1rem',cursor:'pointer'}}>
            <Typography variant='h6'>
                Chat Room from 1
            </Typography>
            <Typography variant="subtitle1">created By blund</Typography>
        </div>
    )
}